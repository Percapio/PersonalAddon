1. §4.4 | Undefined reference | The mechanism for obtaining a spell icon is referred to as "texture lookup" and "One call", but the actual client API function to be used (e.g., `GetSpellTexture` or `C_Spell.GetSpellTexture`) is never specified. | Specify the exact API function used to retrieve the texture by spellID.
2. §5 | Ambiguity | `read_own_breakdown` wraps `GetCombatSessionSourceFromType` but omits `meterType` in its signature, leaving it ambiguous which `Enum.DamageMeterType` (e.g., `DamageDone` vs `Dps`) is actually queried. | Add `meterType` to the signature or explicitly state that `Enum.DamageMeterType.DamageDone` is hardcoded.
3. §5 vs §9 | Contradiction | Section 5 states that a refused session yields "Absent" (which translates to an empty list without faulting), while Section 9 states that if `GetCombatSessionSourceFromType` refuses, the system enters a "Faulted, reason named" state. | Clarify whether API refusal is treated as a hard Fault or gracefully handled as an empty session.
4. §6 | Untested assumption | The design assumes `PlayerFrame` is unconditionally available as an anchor. In custom UI environments (e.g., ElvUI), `PlayerFrame` may not exist, which would cause `SetPoint` to throw a Lua error or anchor incorrectly. | Add an existence check for `PlayerFrame` prior to anchoring, or provide a fallback anchor.
5. §7 | Missing branch | The state diagram initializes with `[*] --> Idle`, missing the edge case where the addon is loaded or the UI is reloaded while the player is already in combat (i.e., `InCombatLockdown()` is true). | Add a startup check to transition directly to `Combat` if already in combat.
6. §7 | Lifecycle leak | The "optional slow refresh" in the `Combat` state lacks an explicit teardown mechanism. Transitioning from `Combat` to `Idle` on `PLAYER_REGEN_ENABLED` must explicitly cancel the timer/ticker to prevent background polling from leaking out of combat. | Specify the cancellation mechanism for the slow-refresh timer upon exiting the `Combat` state.
7. §9 | Missing branch | The degradation table relies on "manual refresh only" as a fallback if `PLAYER_REGEN_ENABLED` is undeliverable, but no user-facing manual refresh mechanism (e.g., slash command or click handler) is defined anywhere in the document. | Define the interaction mechanism for triggering a manual refresh, or remove it as a fallback.
8. §11 | Lifecycle leak | Exit Criterion 7 dictates releasing pooled rows when disabling the feature, but omits the necessary unbinding of `PLAYER_REGEN_ENABLED` and `PLAYER_REGEN_DISABLED` events. | Include event unregistration in the explicit feature teardown sequence.
