1. 4.3 | Undefined reference | The type `FunctionName` is used in `PlateMutabilityVerdict` but is not defined in the document. | Define `FunctionName` (e.g., as a string alias).
2. 5 | Undefined reference | The types `StyleLedger`, `Widget`, and `StyleValue` are used in the signatures for `remember_original` and `restore_all` but are not defined. | Provide definitions for these domain types.
3. 5 | Missing branch | `remember_original` records the original value only once per frame identity. Section 4.3 states Blizzard re-sets size/anchoring on "target change and scale change". Restoring a single static original on disable will write back a stale base value if Blizzard dynamically adjusts it based on unit state. | How should the ledger handle dynamically updated base values? Should original records be invalidated when a frame is recycled for a new unit?
4. 6 | Missing branch | The state diagram lacks transitions for a unit's attackability changing while its nameplate is active (e.g., an unattackable unit becomes attackable via PvP flag or mind control). There is no `Skipped <--> Styled` path. | Add transitions between `Skipped` and `Styled` triggered by faction or reaction changes.
5. 6.1 | Missing branch | The state diagram in Section 6 shows `Styled --> Untracked` for "zone sweep" and "feature disabled", but omits these transitions for the `Skipped` state. If `Skipped` plates are tracked, they also need to be untracked on zone sweep. | Add `Skipped --> Untracked` transitions for zone sweeps and feature disable.
6. 6.1 | Undefined reference | The type `TrackedPlate` is used in `sweep_stale_plates`, `apply_plate_style`, and `sweep_aggro`, but is never defined. | Define `TrackedPlate`.
7. 6.1 | Lifecycle leak | `restore_all` in Section 5 empties the style ledger, but there is no explicit teardown path mentioned for clearing the `trackedPlates` collection when the feature is disabled. | Specify that `disable()` empties the `trackedPlates` list along with restoring the ledger.
8. 7 | Undefined reference | The types `Length` and `PlateUnstyleable` are used in the signatures for `apply_plate_style` and `NamePlateStyle` but are not defined. | Define `Length` and `PlateUnstyleable`.
9. 7.1 | Lifecycle leak | Section 7.1 proposes a "Secure post-hook" to re-assert styles. In the WoW API, `hooksecurefunc` cannot be unhooked. The document does not specify a mechanism to prevent the permanent hook from continuing to re-assert styles after the feature is disabled, which would defeat `restore_all`. | Specify that the post-hook must check if the feature is currently enabled before re-asserting styles.
10. 8 | Undefined reference | The type `NameTextImmovable` is used in the signature for `apply_name_position` but is not defined. | Define `NameTextImmovable`.
11. 9.1 | Missing branch | The state mapping table does not specify the outcome when a mob is targeting another NPC (e.g., town guards or mob infighting). | Specify whether this maps to `Elsewhere` or `Unknown`.
12. 9.1 | Missing branch | If `classify_aggro` evaluates to `Unknown` for a plate that previously held an active aggro colour (e.g., Red), the document specifies to "leave the plate alone". This leaves our stale custom colour applied rather than reverting to Blizzard's original colour. | Specify whether transitioning to `Unknown` should restore the frame's original colour from the ledger or keep the stale custom colour.
13. 9.2 | Contradiction | Section 9.2 states `sweep_aggro` writes to "every tracked plate". However, Section 6 indicates `Skipped` plates (unattackable or unreachable frames) are also part of the tracked lifecycle. `sweep_aggro` would erroneously attempt to colour `Skipped` plates. | Clarify that `sweep_aggro` filters for `Styled` plates only, or redefine `trackedPlates` to exclude `Skipped` ones.
14. 10 | Contradiction | Section 1 claims "every client capability this phase depends on is probed". Section 10 lists the "Nameplate-added event" as a required capability that faults the feature if withheld, but Section 4 (Capability spike) contains no probe for this event. | Add a probe for the nameplate-added event to Section 4, or remove it from the degradation matrix.
