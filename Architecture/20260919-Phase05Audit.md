1. 4.2 | Undefined reference | The return type `CapabilityVerdict` for `probe_combat_protection` is used but not defined anywhere in the document. | Define `CapabilityVerdict`.
2. 5 | Undefined reference | The types `CVarSteward`, `CVarWriteRefused`, and `RestoreReport` are used in the function signatures for `steward_apply` and `steward_restore_all` but are missing from the type declarations. | Provide type definitions for `CVarSteward`, `CVarWriteRefused`, and `RestoreReport`.
3. 5.3 and 6 | Lifecycle leak | The document does not specify what happens if the feature is disabled while a CVar write is in the combat deferral queue (the `Deferred` state). If the queued write is not cancelled, it will execute upon leaving combat, modifying the CVar after the feature was disabled. | Specify that disabling clears any pending combat deferrals, and add a `Deferred --> [*]: disable, queue cleared` transition to the state diagram.
4. 5.3 vs 6 | Contradiction | Section 5.3 states that if a write is refused in combat, "the steward queues it" (placing queue responsibility in the substrate). Conversely, Section 6 places the `Deferred` state in the feature layer's state diagram, implying the feature manages the deferral. | Clarify which layer owns the combat deferral queue and the corresponding state tracking.
5. 5 and 5.3 | Ambiguity | If the steward queues deferred writes, the `Result<Unit, CVarWriteRefused>` return type of `steward_apply` is ambiguous. It is unclear if a deferred write returns `Unit` (hiding it from the caller), `CVarWriteRefused` (which normally means total failure), or if a missing `Deferred` variant should be returned. | Update the return type of `steward_apply` to explicitly account for deferred writes.
6. 6 | Undefined reference | The types `CameraPitch` and `PitchUnavailable` are used in the signature for `apply_camera_pitch` but are not defined. | Define `CameraPitch` and `PitchUnavailable`.
7. 6 | Missing branch | The state diagram shows `[*] --> Applied: enable, CVar writable` but omits the initialization path where the feature is enabled while the player is *already* in combat and writes are refused. | Add a `[*] --> Deferred: enable in combat (writes refused)` transition to the state diagram.
8. 3 vs 6 | Contradiction | Section 3 claims the feature has "no state machine" and is "applied once on login and left alone." However, Section 6 introduces an explicit state machine diagram with multiple states and transitions for settings changes ("setting changed, re-applied"). | Remove the "no state machine" claim from Section 3 and clarify that while it lacks per-frame work, it maintains a state machine for lifecycle events.
