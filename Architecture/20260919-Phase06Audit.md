1. 4 | Undefined reference | `CapabilityVerdict` is used as the type for properties in `SettingsApiVerdict` but is never defined in the document. | Define `CapabilityVerdict` (e.g., as an enum or boolean).
2. 4 & 7 | Contradiction | Section 7 states that nameplate colour settings are "Exposed only if §4 reports a colour control", but the `SettingsApiVerdict` struct in Section 4 does not probe for any colour controls. | Add a probe for colour controls to `SettingsApiVerdict` in Section 4, or remove the conditional exposure in Section 7.
3. 5 | Undefined reference | `Absent`, `ChoiceOption`, and `SchemaViolation` are used in type signatures for the configuration schema but are not defined anywhere in the document. | Define these types so the schema structure and error bounds are explicit.
4. 8 | Contradiction | The section header claims "Dead-key removal is the store's first real migration", but the immediate body text contradicts this by stating "Removing nameOffsetY needs no migration — the prune handles it." | Reconcile the text by either clarifying why a prune is considered a migration in this architecture, or rewording the header.
5. 9 | Undefined reference | `PanelHandle` and `PanelUnavailable` are used as return types for `build_settings_panel` but are not defined in the document. | Define the structure of these return types.
6. 9 | Missing branch | The sequence diagram for panel interactions models only the successful validation path (`Accepted`). It completely omits the error path for when `validate_against_schema` returns a `SchemaViolation`. | Add an alternative path (`alt`) to the sequence diagram to demonstrate how the Panel handles and surfaces a schema validation failure to the user.
7. 10 | Untested assumption | The checklist assumes that the addon can clear the `PersonalAddonProbeLog` data on load while simultaneously removing it from the `.toc` saved variables list. In the WoW client, removing a variable from the `.toc` prevents it from being loaded into the Lua environment, making it impossible to clear the file on disk via Lua. | Keep the variable in the `.toc` for this phase to allow clearing it, and schedule its removal from the `.toc` for a subsequent phase.
8. 12 | Missing branch | The failure modes table asserts that if a feature faults while the panel is open, the "Panel shows it as faulted rather than as off." However, no mechanism (e.g., event listener, polling, state transition) is described for the Panel to receive dynamic fault notifications from the Registry. | Specify the mechanism the Panel uses to react to live feature faults while open.
9. 12 | Lifecycle leak | If the Panel dynamically subscribes to Registry fault events to satisfy the requirement in Section 12, there is no explicit unbind, teardown, or disposal path specified for when the settings panel is closed. | Explicitly define the teardown/unbind path for active panel subscriptions upon panel closure to prevent event listener leaks.
