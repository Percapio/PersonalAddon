1. 4, 5.2 | Contradiction | Section 4's sequence diagram shows `hydrate saved table` running before `migrate to current schema`. However, Section 5.2 describes `hydrate_config_store` as applying migrations and then merging defaults. If hydration merges defaults and discards type mismatches before migration runs, old schema values with changed types are destroyed before the migration step can read them. | Clarify the execution order: load from disk -> migrate -> merge defaults.
2. 5.2 | Missing branch | The default merge rule specifies how to handle keys present in both the persisted store and defaults, and keys absent from the persisted store. It does not state whether persisted keys that are absent from defaults (obsolete keys) are preserved or pruned. | Specify whether obsolete keys are retained or discarded during hydration.
3. 5.3 | Undefined reference | The `MigrationStep` type is used in the `migrate_store` signature but its structure and contract are never defined. | Define the `MigrationStep` type signature.
4. 6.1 | Missing branch | The state machine does not define transitions for failures during lifecycle hooks other than event dispatch. If `enable` fails (which Section 13 says keeps the feature disabled) or if `onConfigChanged` raises an error, the system's behavior and state transition for the feature are unspecified. | Add transitions for `enable` and `onConfigChanged` failures, and specify if `disable` is invoked to clean up partial initialization.
5. 6.1 | Missing branch | The lifecycle contract states: "A feature that cannot apply a key live returns to the caller after recording that a reload is pending". However, `onConfigChanged` returns `Unit` and no registry API is defined to record this pending reload state. | Define a `registry.request_reload()` API or change `onConfigChanged` to return a `ReloadRequired` result type.
6. 7 | Undefined reference | Section 7 states features "subscribe through the dispatcher", but no subscription API (e.g., `dispatcher.subscribe`) is defined. Additionally, the `FeatureSubscription` type used in `dispatch_event` is undefined. | Define the event subscription API and the `FeatureSubscription` type.
7. 6.1, 7 | Lifecycle leak | The contract states "After `disable()` returns, the feature holds zero registered events". Since features subscribe through the dispatcher, they must unsubscribe when disabled. Without an unbind or unsubscribe API defined on the dispatcher, a feature cannot fulfill this contract. | Define an `unsubscribe` API on the dispatcher for features to use in their `disable` methods.
8. 8 | Undefined reference | Section 8 states "Capacity is a construction parameter" but provides no constructor function for `FramePool` to accept this parameter. | Add a `create_pool(capacity: integer, dropPolicy: DropPolicy) -> FramePool` function.
9. 8 | Ambiguity | The drop policy behavior is ambiguous: "A combat text pool that hits its cap should drop and keep going". It is unclear if the pool internally drops the oldest frame (eviction) and successfully returns a reused frame, or if `acquire_frame` returns `PoolExhausted` and the *caller* must skip its rendering attempt. | Clarify whether the drop policy is implemented via internal pool eviction or caller-side error handling.
10. 11.2 | Untested assumption | The classification logic assumes `SPELL_ENERGIZE` events will always arrive before the corresponding `UNIT_POWER_UPDATE` mana delta. In the WoW client, combat log events can be delayed or processed in different buckets. If the energize event arrives after the mana delta, the delta will be falsely classified as a `SpiritTick`. | Buffer `UNIT_POWER_UPDATE` events briefly to ensure the correlation window can catch slightly delayed combat log events.
11. 11.4 | Missing branch | The state machine lacks a transition from `Unknown` to `InFsr`. If the player casts a spell with a mana cost immediately after logging in or zoning (while in the `Unknown` state), there is no path to enter `InFsr` to begin tracking the five-second window. | Add the transition `Unknown --> InFsr: cast succeeds with modified cost > 0`.
12. 3, 11.4 | Untested assumption | Section 3 asserts "FSR render — binary... No partial-regen state". This assumes spirit regen cannot happen during FSR. Talents like Mage Armor permit partial spirit regen while casting. Therefore, a `SpiritTick` will be observed while the state machine is in `InFsr`. Since there is no `InFsr --> InFsr: SpiritTick observed` transition, the event is dropped and the phase estimate will visibly drift once `Regenerating` resumes. | Handle the `SpiritTick` event in the `InFsr` state to maintain an accurate phase estimate during partial regen.
